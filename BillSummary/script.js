document.addEventListener("DOMContentLoaded", function () { const itemsTable = document.getElementById("itemsTable"); const addItemBtn = document.getElementById("addItemBtn"); const generateBillBtn = document.getElementById("generateBillBtn"); const totalAmount = document.getElementById("totalAmount"); const shopName = document.getElementById("shopName"); const billPreview = document.getElementById("billPreview"); const previewContent = document.getElementById("previewContent"); const whatsappBtn = document.getElementById("whatsappBtn"); const resetBtn = document.getElementById("resetBtn"); /* ========================= CREATE TABLE ROW ========================= */ function createRow() { const row = document.createElement("tr"); row.innerHTML = ` <td> <input type="text" class="item-name" placeholder="Enter service / item" > </td> <td> <input type="number" class="price-input" placeholder="0" min="0" step="1" inputmode="numeric" > </td> <td> <button type="button" class="remove-btn" title="Remove item" > × </button> </td> `; const priceInput = row.querySelector(".price-input"); const removeBtn = row.querySelector(".remove-btn"); priceInput.addEventListener( "input", calculateTotal ); removeBtn.addEventListener( "click", function () { const rows = itemsTable.querySelectorAll("tr"); if (rows.length > 1) { row.remove(); calculateTotal(); } } ); itemsTable.appendChild(row); } /* ========================= CREATE DEFAULT 5 ROWS ========================= */ for (let i = 0; i < 5; i++) { createRow(); } /* ========================= ADD ITEM ========================= */ addItemBtn.addEventListener( "click", function () { createRow(); calculateTotal(); } ); /* ========================= CALCULATE TOTAL ========================= */ function calculateTotal() { const prices = itemsTable.querySelectorAll( ".price-input" ); let total = 0; prices.forEach( function (input) { const value = Number(input.value); if ( !isNaN(value) && value > 0 ) { total += value; } } ); totalAmount.textContent = formatAmount(total); } /* ========================= FORMAT RUPEES ========================= */ function formatAmount(amount) { return ( "₹" + Number(amount).toLocaleString( "en-IN" ) ); } /* ========================= GET ITEMS ========================= */ function getItems() { const rows = itemsTable.querySelectorAll("tr"); const items = []; rows.forEach( function (row) { const name = row.querySelector( ".item-name" ).value.trim(); const price = Number( row.querySelector( ".price-input" ).value ); if ( name !== "" && !isNaN(price) && price >= 0 ) { items.push({ name: name, price: price }); } } ); return items; } /* ========================= GENERATE BILL ========================= */ generateBillBtn.addEventListener( "click", function () { const shop = shopName.value.trim(); const items = getItems(); if (shop === "") { alert( "Please enter the shop name." ); shopName.focus(); return; } if (items.length === 0) { alert( "Please enter at least one service or item." ); return; } let total = 0; let html = ` <div class="preview-shop"> ${escapeHtml(shop)} </div> `; items.forEach( function (item) { total += item.price; html += ` <div class="preview-row"> <span> ${escapeHtml(item.name)} </span> <span> ${formatAmount(item.price)} </span> </div> `; } ); html += ` <div class="preview-total"> <span>Total</span> <span> ${formatAmount(total)} </span> </div> `; previewContent.innerHTML = html; billPreview.classList.remove( "hidden" ); billPreview.scrollIntoView({ behavior: "smooth", block: "start" }); } ); /* ========================= WHATSAPP ========================= */ whatsappBtn.addEventListener( "click", function () { const shop = shopName.value.trim(); const items = getItems(); if ( shop === "" || items.length === 0 ) { return; } let total = 0; let message = "🧾 *Bill Summary*\n\n"; message += "*" + shop + "*\n\n"; items.forEach( function (item) { total += item.price; message += item.name + " - " + formatAmount(item.price) + "\n"; } ); message += "\n*Total: " + formatAmount(total) + "*"; message += "\n\n🌱 Digital Bill Summary"; const url = "https://wa.me/?text=" + encodeURIComponent(message); window.open( url, "_blank" ); } ); /* ========================= RESET ========================= */ resetBtn.addEventListener( "click", function () { shopName.value = ""; itemsTable.innerHTML = ""; for (let i = 0; i < 5; i++) { createRow(); } calculateTotal(); previewContent.innerHTML = ""; billPreview.classList.add( "hidden" ); window.scrollTo({ top: 0, behavior: "smooth" }); } ); /* ========================= ESCAPE HTML ========================= */ function escapeHtml(text) { const div = document.createElement("div"); div.textContent = text; return div.innerHTML; } /* INITIAL TOTAL */ calculateTotal(); }); 
